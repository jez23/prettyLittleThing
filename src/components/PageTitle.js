import React from 'react';

const PageTitle = ({ title }) => {
    return (
        <section className="pageTitle">
            <h3>{ title }</h3>
        </section>
    )
}

export default PageTitle;